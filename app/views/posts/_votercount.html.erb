 
  <b>Voters (<%= @voters.count %>):</b>
    <div class="horiz"> 
      <% @voters.each do |v| %>
            <%= link_to image_tag(v.image, class: 'imooge'), user_path(v) %>
      <% end %>
    </div>